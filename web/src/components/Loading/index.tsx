import React from 'react';

// type Props = {
//   relative?: boolean;
//   size?: number;
// };

function Loading(/*{ relative, size = 40 }: Props*/) {
  return (
    <div> wait while loading page11 ... </div>
  );
}

export default Loading;